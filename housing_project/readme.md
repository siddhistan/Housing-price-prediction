How to install them all at once
Once you have saved that text into your requirements.txt file in the root directory, you can install everything in your terminal with one command:

Bash
pip install -r requirements.txt